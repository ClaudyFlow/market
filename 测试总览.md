# API 可用性检测注解 - 测试总览

## 📁 测试文件结构

```
d:\market/
├── run-all-tests.bat                    # ★ 一键运行所有测试（推荐）
├── test-availability.bat                # 集成测试脚本
│
├── backend/src/test/java/com/market/
│   ├── aspect/
│   │   ├── ApiAvailabilityIntegrationTest.java  # 集成测试
│   │   ├── ApiAvailabilityAnnotationTest.java   # 注解测试
│   │   ├── TEST_GUIDE.md                        # 测试指南
│   │   └── 完整测试流程.md                      # 详细测试流程
│   │
│   └── common/
│       └── ApiStatusCodeTest.java               # 状态码测试
│
└── backend/src/main/java/com/market/
    ├── controller/
    │   ├── ApiAvailabilityTestController.java   # 测试控制器（10 个测试端点）
    │   └── ApiHealthController.java             # 健康监控控制器（4 个端点）
    │
    └── annotation/
        ├── 如何测试.md                          # 快速测试说明
        └── 改进说明.md                          # 改进说明
```

---

## 🚀 快速测试（3 步）

### 方法 1: 一键测试（推荐）

```bash
cd d:\market
run-all-tests.bat
```

**自动执行：**
1. ✅ 运行注解集成测试
2. ✅ 运行状态码测试
3. ✅ 检查服务状态（如运行则询问是否进行集成测试）

---

### 方法 2: 分步测试

#### 步骤 1: 单元测试（无需启动服务）

```bash
cd d:\market\backend\src\test\java

# 运行注解测试
java com.market.aspect.ApiAvailabilityIntegrationTest

# 运行状态码测试
java com.market.common.ApiStatusCodeTest
```

#### 步骤 2: 启动服务

**IDE:** 右键运行 `MarketApplication.java`

**命令行:** `mvn spring-boot:run -pl backend`

#### 步骤 3: 集成测试

```bash
cd d:\market
test-availability.bat
```

---

## 📋 测试端点列表

启动服务后，可访问以下端点进行测试：

### 可用性测试端点

| 端点 | 说明 | 预期结果 |
|------|------|----------|
| `GET /api/test/availability/basic` | 基础测试 | 成功 |
| `GET /api/test/availability/database` | 数据库依赖检测 | 取决于数据库 |
| `GET /api/test/availability/redis` | Redis 依赖检测 | 取决于 Redis |
| `GET /api/test/availability/multi-dependencies` | 多依赖检测 | 取决于服务 |
| `GET /api/test/availability/throw-exception` | 失败抛异常 | 可能返回错误 |
| `GET /api/test/availability/continue-on-failure` | 失败继续执行 | 总是成功 |
| `GET /api/test/availability/timeout-test?delayMs=2000` | 超时测试 | 1 秒后返回超时 |
| `GET /api/test/availability/retry-test` | 重试机制测试 | 成功或重试 |
| `GET /api/test/availability/health-check` | 健康检查测试 | 成功 |
| `GET /api/test/availability/disabled` | 禁用检测测试 | 成功 |

### 健康监控端点

| 端点 | 说明 |
|------|------|
| `GET /api/health/check` | 简化健康检查 |
| `GET /api/health/check/detailed` | 详细健康检查 |
| `GET /api/health/status` | 所有服务状态 |
| `GET /api/health/status/{serviceName}` | 指定服务状态 |

---

## 🧪 测试用例

### 1. 基础功能测试

```bash
curl http://localhost:8080/api/test/availability/basic
```

**预期：**
```json
{"code":200,"message":"基础测试通过","data":"基础测试通过"}
```

### 2. 数据库依赖测试

```bash
curl http://localhost:8080/api/test/availability/database
```

**预期（数据库可用）：**
```json
{"code":200,"message":"数据库测试通过"}
```

**预期（数据库不可用）：**
```json
{"code":4101,"message":"依赖服务 [database] 不可用"}
```

### 3. 超时控制测试

```bash
curl -m 5 http://localhost:8080/api/test/availability/timeout-test?delayMs=2000
```

**预期：** 1 秒后返回（不是 2 秒）
```json
{"code":4001,"message":"方法执行超时（超过 1000ms）"}
```

### 4. 健康状态查询

```bash
curl http://localhost:8080/api/health/status
```

**预期：**
```json
{
  "code": 200,
  "data": {
    "timestamp": "2024-01-01T12:00:00",
    "status": "UP",
    "services": {
      "api": {"name": "API 服务", "status": "UP"},
      "database": {"name": "数据库", "status": "UP"},
      "redis": {"name": "Redis", "status": "UP"}
    }
  }
}
```

---

## ✅ 测试检查清单

### 单元测试
- [ ] 注解存在性测试通过
- [ ] 注解属性配置正确
- [ ] 状态码枚举值正确
- [ ] 失败策略枚举正确
- [ ] 健康检查注解正确

### 编译测试
- [ ] 代码编译无错误
- [ ] 无编译警告

### 集成测试
- [ ] 服务启动成功
- [ ] 端口 8080 可访问
- [ ] 基础接口返回正常
- [ ] 数据库依赖检测正常
- [ ] Redis 依赖检测正常
- [ ] 超时控制在 1 秒返回
- [ ] THROW 策略抛出异常
- [ ] RETURN_ERROR 策略返回错误
- [ ] CONTINUE 策略继续执行
- [ ] 健康状态查询返回正确数据

### 日志验证
- [ ] 成功检测日志：`API 可用性检测通过`
- [ ] 失败检测日志：`API 可用性检测失败`
- [ ] 超时日志：`方法执行超时`

---

## 📊 状态码验证

| 状态码 | 说明 | 测试端点 |
|--------|------|----------|
| 2000 | 服务可用 | `/api/test/availability/basic` |
| 2001 | 降级模式 | `/api/test/availability/continue-on-failure` |
| 3000 | 正在检测 | (日志中查看) |
| 4001 | 服务超时 | `/api/test/availability/timeout-test` |
| 4101 | 数据库不可用 | `/api/test/availability/database` (数据库停用时) |
| 4102 | Redis 不可用 | `/api/test/availability/redis` (Redis 停用时) |

---

## 🐛 常见问题

### Q: 测试失败 "连接被拒绝"
**A:** 服务未启动，先运行 `MarketApplication.java`

### Q: 404 Not Found
**A:** 
1. 确认服务已完全启动
2. 确认 URL 路径正确
3. 查看控制台映射信息

### Q: 注解不生效
**A:**
1. 确认方法上有 `@ApiAvailable`
2. 确认 Aspect 类有 `@Aspect` 和 `@Component`
3. 查看日志是否有检测信息

### Q: 超时测试不超时
**A:** 确保 `delayMs` 参数大于 timeout 值（默认 1000ms）

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| `完整测试流程.md` | 详细的四阶段测试流程 |
| `TEST_GUIDE.md` | 测试指南和排查方法 |
| `如何测试.md` | 快速测试说明 |
| `API_AVAILABILITY_COMPLETE.md` | 完整功能文档 |
| `改进说明.md` | 基于消息库的改进说明 |

---

## 🎯 推荐测试流程

```
1. 运行 run-all-tests.bat
   ↓
2. 查看测试结果
   ↓
3. 如有失败，查看详细错误
   ↓
4. 启动服务（如需集成测试）
   ↓
5. 运行 test-availability.bat
   ↓
6. 查看完整测试流程.md 进行详细测试
```

---

## 📞 需要帮助？

查看详细文档：`backend\src\test\java\com\market\aspect\完整测试流程.md`
