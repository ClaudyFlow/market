# 如何启动后端服务

## 问题：连接被拒绝 (Connection Refused)

这说明后端服务没有运行。你需要先启动服务才能测试 API。

---

## 启动方法

### 方法 1: 使用 IDE 启动（推荐）

#### IntelliJ IDEA
1. 打开项目 `d:\market`
2. 找到文件：`backend/src/main/java/com/market/MarketApplication.java`
3. 右键点击文件 → **Run 'MarketApplication'**
4. 等待控制台显示 `Started MarketApplication in X seconds`

#### Eclipse
1. 打开项目 `d:\market`
2. 找到文件：`backend/src/main/java/com/market/MarketApplication.java`
3. 右键点击文件 → **Run As** → **Java Application**
4. 等待启动完成

#### VS Code
1. 打开项目 `d:\market`
2. 打开 `MarketApplication.java`
3. 点击代码上方的 **Run** 按钮
4. 等待启动完成

---

### 方法 2: 使用 Maven 命令行

如果已安装 Maven：

```bash
cd d:\market
mvn spring-boot:run -pl backend
```

---

### 方法 3: 使用 Gradle（如果有）

```bash
cd d:\market
gradle :backend:bootRun
```

---

## 验证服务启动成功

### 方法 1: 查看日志

启动成功后应该看到类似输出：
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.4.0)

...
Started MarketApplication in 5.123 seconds
```

### 方法 2: 检查端口

```bash
# Windows
netstat -ano | findstr :8080

# 应该看到类似输出：
# TCP    0.0.0.0:8080    0.0.0.0:0    LISTEN    12345
```

### 方法 3: 访问测试接口

浏览器访问：
```
http://localhost:8080/api/test/availability/basic
```

或者使用 curl：
```bash
curl http://localhost:8080/api/test/availability/basic
```

成功响应：
```json
{"code":200,"message":"基础测试通过","data":"基础测试通过"}
```

---

## 常见问题

### Q: 端口 8080 已被占用

**解决方案：** 修改端口

在 `backend/src/main/resources/application.properties` 或 `application.yml` 中添加：
```properties
server.port=8081
```

然后测试时使用新端口：
```bash
curl http://localhost:8081/api/test/availability/basic
```

### Q: 启动失败 - 数据库连接错误

**解决方案：** 使用 H2 内存数据库（开发环境）

确保 `application.properties` 中有：
```properties
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
```

### Q: 找不到 main 方法

**解决方案：** 确认文件路径正确
- 文件：`backend/src/main/java/com/market/MarketApplication.java`
- 类名：`MarketApplication`
- 方法：`public static void main(String[] args)`

---

## 启动后测试

服务启动成功后，运行测试脚本：
```bash
test-availability.bat
```

或者手动测试：
```bash
curl http://localhost:8080/api/test/availability/basic
```
