# 🚀 如何启动后端服务

## 问题诊断

你遇到的错误：`http://localhost:8080/api/test/availability/basic 拒绝连接`

**原因：** 后端服务没有运行。需要先启动服务才能测试 API。

---

## ✅ 启动方法（3 种）

### 方法 1: 使用 IntelliJ IDEA（最简单）

1. **打开项目**
   - 启动 IntelliJ IDEA
   - File → Open → 选择 `d:\market` 文件夹

2. **运行应用**
   - 找到文件：`backend/src/main/java/com/market/MarketApplication.java`
   - 右键点击文件中的 `main` 方法
   - 选择 **Run 'MarketApplication.main()'**

3. **等待启动完成**
   - 控制台显示 `Started MarketApplication in X seconds` 表示成功

4. **测试接口**
   - 浏览器访问：`http://localhost:8080/api/test/availability/basic`

---

### 方法 2: 使用 Eclipse

1. **打开项目**
   - 启动 Eclipse
   - File → Import → Existing Maven Projects
   - 选择 `d:\market` 目录

2. **运行应用**
   - 找到文件：`MarketApplication.java`
   - 右键 → Run As → Java Application

3. **等待启动完成**

---

### 方法 3: 使用命令行（需要 Maven）

如果已安装 Maven：

```bash
cd d:\market
mvn spring-boot:run -pl backend
```

**如果没有 Maven：**

1. 先安装 Maven：https://maven.apache.org/download.cgi
2. 或者使用方法 1（IDE）启动

---

## 🔍 验证服务启动成功

### 检查方法 1: 查看日志

启动成功后日志最后会显示：
```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v3.4.0)

...
Started MarketApplication in 5.123 seconds (process running for 6.789)
```

### 检查方法 2: 检查端口

打开命令提示符，输入：
```bash
netstat -ano | findstr :8080
```

如果看到类似输出，说明服务正在运行：
```
TCP    0.0.0.0:8080    0.0.0.0:0    LISTEN    12345
TCP    [::]:8080       [::]:0       LISTEN    12345
```

### 检查方法 3: 测试接口

```bash
# 浏览器访问
http://localhost:8080/api/test/availability/basic

# 或使用 curl
curl http://localhost:8080/api/test/availability/basic
```

**成功响应：**
```json
{"code":200,"message":"基础测试通过","data":"基础测试通过"}
```

---

## ⚠️ 常见问题

### 1. 端口 8080 被占用

**错误信息：** `Port 8080 is already in use`

**解决方案：** 修改端口

在 `backend/src/main/resources/application.properties` 中添加：
```properties
server.port=8081
```

然后使用新端口测试：
```bash
curl http://localhost:8081/api/test/availability/basic
```

### 2. 数据库连接失败

**错误信息：** `Cannot determine embedded database driver class`

**解决方案：** 项目已配置使用 H2 内存数据库，确保：
```properties
spring.profiles.active=h2
```

### 3. 找不到或无法加载主类

**解决方案：** 
1. 先编译项目：`mvn compile -pl backend`
2. 或在 IDE 中执行 Build → Rebuild Project

### 4. Java 版本不兼容

**要求：** Java 21+

检查版本：
```bash
java -version
```

如果版本过低，请下载安装：https://adoptium.net/

---

## 📝 启动后测试

服务启动成功后，运行测试脚本：

```bash
# Windows
test-availability.bat

# 或手动测试
curl http://localhost:8080/api/test/availability/basic
curl http://localhost:8080/api/test/availability/database
curl http://localhost:8080/api/test/availability/timeout-test?delayMs=2000
```

---

## 📞 需要帮助？

如果以上方法都无法解决问题，请检查：

1. Java 是否安装：`java -version`
2. 项目依赖是否完整：`backend/src/main/java` 目录下应有所有源代码
3. IDE 是否正确导入项目

---

## 快速参考

| 操作 | 命令/步骤 |
|------|----------|
| 启动服务（IDE） | 右键 MarketApplication.java → Run |
| 启动服务（命令行） | `mvn spring-boot:run -pl backend` |
| 测试接口 | `curl http://localhost:8080/api/test/availability/basic` |
| 检查端口 | `netstat -ano \| findstr :8080` |
| 查看日志 | IDE 控制台或 `backend/logs/` 目录 |
