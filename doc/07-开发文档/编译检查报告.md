# 编译检查报告

> **检查时间**: 2026-04-06 14:28
> **检查范围**: 后端 + 前端
> **构建工具**: mvnd (Maven Daemon) + npm

---

## 📊 编译总览

| 模块 | 状态 | 构建工具 | 耗时 |
|------|------|----------|------|
| 后端 (backend) | ✅ 成功 | mvnd | 6.686s |
| 前端 (frontend) | ✅ 成功 | npm/vite | 15.79s |

---

## 🔧 后端编译详情

### 编译命令
```bash
mvnd clean compile -DskipTests
```

### 编译结果
- **状态**: ✅ BUILD SUCCESS
- **编译文件数**: 234 个 Java 源文件
- **Java 版本**: 21 (release 21)
- **总耗时**: 6.686s

### ⚠️ 编译警告 (14 个)

#### 1. Lombok 字段重复警告 (12 个)
以下 Aspect 类中存在 `log` 字段重复定义的问题：

| 文件 | 行号 | 警告信息 |
|------|------|---------|
| `DeprecatedApiAspect.java` | 28 | Field 'log' already exists |
| `IdempotentAspect.java` | 29 | Field 'log' already exists |
| `SensitiveDataAspect.java` | 38 | Field 'log' already exists |
| `ChatLogAspect.java` | 14 | Field 'log' already exists |
| `DataScopeAspect.java` | 24 | Field 'log' already exists |
| `LogTestController.java` | 15 | Field 'log' already exists |
| `AuditLogAspect.java` | 37 | Field 'log' already exists |
| `RetryableAspect.java` | 23 | Field 'log' already exists |
| `ApiAvailabilityAspect.java` | 37 | Field 'log' already exists |
| `ReviewLogAspect.java` | 14 | Field 'log' already exists |
| `DistributedLockAspect.java` | 32 | Field 'log' already exists |
| `CacheableAspect.java` | 28 | Field 'log' already exists |

**原因**: 这些类使用了 `@Slf4j` 或 `@Log` 等 Lombok 注解自动生成 `log` 字段,但又手动定义了同名 `log` 字段。

**建议**: 移除手动定义的 `log` 字段,仅保留 Lombok 注解。

#### 2. 过时 API 警告 (2 个)

| 文件 | 行号 | 警告信息 |
|------|------|---------|
| `RedisConfig.java` | 31 | `Jackson2JsonRedisSerializer.setObjectMapper()` 已过时 |

**原因**: 使用了 Spring Data Redis 中标记为待删除的方法。

**建议**: 使用 `Jackson2JsonRedisSerializer` 的构造函数传入 `ObjectMapper`,或使用 `GenericJackson2JsonRedisSerializer`。

#### 3. 未检查操作警告

| 文件 | 警告信息 |
|------|---------|
| `OrderController.java` | 使用了未经检查或不安全的操作 |

**建议**: 使用 `-Xlint:unchecked` 重新编译以获取详细信息。

---

## 🎨 前端编译详情

### 编译命令
```bash
npm run build
```

### 编译结果
- **状态**: ✅ 成功
- **模块转换数**: 2548 个
- **总耗时**: 15.79s

### 📦 构建产物

#### HTML 文件
| 文件 | 大小 | Gzip 大小 |
|------|------|-----------|
| `admin.html` | 0.88 kB | 0.42 kB |
| `merchant.html` | 0.89 kB | 0.43 kB |
| `index.html` | 0.98 kB | 0.47 kB |

#### CSS 文件 (部分)
| 文件 | 大小 | Gzip 大小 |
|------|------|-----------|
| `logger-C-dDSIMp.css` | 352.43 kB | 47.23 kB |
| `user-CnncBU_R.css` | 46.62 kB | 6.60 kB |
| `ProductDetailNew-C910_XnP.css` | 43.05 kB | 7.77 kB |
| `Home-DmXjzmqF.css` | 23.50 kB | 4.43 kB |

#### JavaScript 文件 (部分)
| 文件 | 大小 | Gzip 大小 |
|------|------|-----------|
| `index-C8kWwlUb.js` | 1,034.93 kB | 343.42 kB |
| `vendor-element-Bx_9ugSE.js` | 879.38 kB | 285.08 kB |
| `vendor-vue-BLnifzbD.js` | 109.16 kB | 42.58 kB |
| `vendor-icons-8QSwreE8.js` | 45.38 kB | 11.48 kB |

### ⚠️ 性能优化建议

**Chunk 大小警告**: 部分代码块超过 500KB

| 文件 | 大小 |
|------|------|
| `index-C8kWwlUb.js` | 1,034.93 kB |
| `vendor-element-Bx_9ugSE.js` | 879.38 kB |

**优化建议**:
1. ✅ 使用动态 `import()` 进行代码分割
2. ✅ 配置 `build.rollupOptions.output.manualChunks` 优化 chunk 分割
3. ✅ 调整 `build.chunkSizeWarningLimit` 配置

### ✅ 性能优化已实施 (2026-04-20)

| 优化项 | 状态 | 说明 |
|--------|------|------|
| 代码分割 | ✅ 已完成 | vendor-vue/vendor-element/vendor-icons/vendor-utils 分离打包 |
| 禁用 sourcemap | ✅ 已完成 | 生产环境移除 source map 减少 bundle 体积 |
| Tree Shaking | ✅ 已完成 | 启用未引用代码 elimination |
| Console 移除 | ✅ 已完成 | 生产环境移除 console/debugger |
| 通知中心路由 | ✅ 已完成 | 新增 /notice 路由 |

---

## 🎯 改进建议

### 高优先级 🔴
1. **修复 Lombok 字段重复警告** (12 个文件)
   - 移除手动定义的 `log` 字段
   - 保留 `@Slf4j` 注解即可

2. **更新 Redis 序列化器配置**
   - 替换过时的 `setObjectMapper()` 方法
   - 使用构造函数传入 `ObjectMapper`

### 中优先级 🟡
3. **优化前端 Chunk 大小**
   - 配置 Vite 的 `manualChunks` 策略
   - 对大型依赖进行分包

4. **检查 OrderController 的未检查操作**
   - 使用泛型确保类型安全

### 低优先级 🟢
5. **启用编译详细日志**
   - 使用 `-Xlint:deprecation` 查看所有过时 API
   - 使用 `-Xlint:unchecked` 查看未检查操作

---

## 📝 变更历史

| 版本 | 日期 | 检查工具 | 结果 | 备注 |
|------|------|---------|------|------|
| v1.0.0 | 2026-04-06 14:28 | mvnd + npm | ✅ 通过 | 初始编译检查,存在 14 个警告 |
| v1.1.0 | 2026-04-20 | mvnd + npm | ✅ 通过 | 前端性能优化已完成（代码分割/禁用sourcemap/TreeShaking/通知中心路由） |

---

*本文档由 AI 自动生成,每次编译检查后更新。*
