# 开发规范

> **创建时间**: 2026-04-06
> **更新时间**: 2026-04-06
> **维护人**: 管理AI
> **版本**: v1.0.0

---

## 📖 1. 概述

本文档规定购物商城系统的开发规范，所有开发人员必须遵守。

---

## 💻 2. 前端规范

### 2.1 命名规范
- **文件名**: 使用 PascalCase (组件) 或 camelCase (工具)
  - ✅ `ProductCard.vue`, `userService.ts`
  - ❌ `product_card.vue`, `User-Service.ts`

- **变量/函数**: 使用 camelCase
  - ✅ `const productList = []`
  - ✅ `function getUserInfo() {}`

- **常量**: 使用 UPPER_SNAKE_CASE
  - ✅ `const API_BASE_URL = '/api'`

### 2.2 组件规范
```vue
<template>
  <!-- 模板内容 -->
</template>

<script setup lang="ts">
// 导入
import { ref, computed } from 'vue'

// 类型定义
interface Props {
  title: string
}

// Props 定义
const props = defineProps<Props>()

// 响应式数据
const count = ref(0)

// 计算属性
const doubleCount = computed(() => count.value * 2)

// 方法
function handleClick() {
  console.log('clicked')
}
</script>

<style scoped>
/* 样式 */
</style>
```

### 2.3 API 调用规范
```typescript
// ✅ 正确：使用统一的 API 模块
import { getProductList } from '@/user/api/product'

// ❌ 错误：直接使用 axios
import axios from 'axios'
axios.get('/api/product')
```

### 2.4 错误处理
```typescript
// 统一错误处理
try {
  const data = await getProductList()
  // 处理成功
} catch (error) {
  ElMessage.error('获取商品列表失败')
  console.error(error)
}
```

---

## 🔧 3. 后端规范

### 3.1 命名规范
- **类名**: PascalCase
  - ✅ `ProductController`, `UserService`
  
- **方法/变量**: camelCase
  - ✅ `getProductById()`, `userList`

- **常量**: UPPER_SNAKE_CASE
  - ✅ `private static final String API_PREFIX = "/api"`

### 3.2 Controller 规范
```java
@RestController
@RequestMapping("/api/product")
public class ProductController {
    
    @Autowired
    private ProductService productService;
    
    /**
     * 获取商品列表
     * @param page 页码
     * @param size 每页数量
     * @return 商品列表
     */
    @GetMapping("/list")
    public Result<List<Product>> getProductList(
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "10") Integer size) {
        return Result.success(productService.getProductList(page, size));
    }
}
```

### 3.3 统一返回格式
```java
public class Result<T> {
    private Integer code;
    private String message;
    private T data;
    
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "成功", data);
    }
    
    public static <T> Result<T> error(String message) {
        return new Result<>(500, message, null);
    }
}
```

### 3.4 异常处理
```java
@ControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusinessException(BusinessException e) {
        return Result.error(e.getMessage());
    }
}
```

---

## 🗂️ 4. Git 规范

### 4.1 分支策略
- **main**: 生产分支
- **dev**: 开发分支
- **feature/xxx**: 功能分支
- **hotfix/xxx**: 修复分支

### 4.2 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

**type 类型**:
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

**示例**:
```
feat(product): 添加商品搜索功能

- 实现商品名称模糊搜索
- 支持分类筛选
- 添加分页功能

Closes #123
```

---

## 📝 5. 注释规范

### 5.1 类注释
```java
/**
 * 商品服务实现类
 * 负责商品的查询、更新等操作
 * 
 * @author 开发团队
 * @since 2026-04-06
 */
@Service
public class ProductServiceImpl implements ProductService {
}
```

### 5.2 方法注释
```java
/**
 * 根据ID获取商品
 * 
 * @param productId 商品ID
 * @return 商品信息
 * @throws BusinessException 商品不存在时抛出
 */
public Product getProductById(Long productId) {
}
```

---

## 🔄 6. 变更历史

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| v1.0.0 | 2026-04-06 | 初始版本 | 管理AI |

---

*本文档每次规范变更时更新 md 版本，typ 版本每晚统一更新。*
