# 404 错误页面配置指南

## 📁 文件说明

| 文件 | 说明 | 位置 |
|------|------|------|
| `404.html` | 自定义 404 错误页面 | `frontend/public/404.html` |
| `nginx.conf` | Nginx 配置文件（Docker） | 项目根目录 |
| `nginx-404-config.conf` | Nginx 配置示例 | 项目根目录 |

---

## 🚀 使用方式

### 方式一：Docker 环境（推荐）

1. **将 404 页面复制到 Nginx 目录**

```bash
# 构建前端项目
cd frontend
npm run build

# 复制 404 页面到构建目录
cp public/404.html build/404.html
```

2. **使用 docker-compose 启动**

```yaml
# docker-compose.yml
version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./frontend/build:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

  backend:
    build: .
    ports:
      - "8080:8080"
```

3. **启动服务**

```bash
docker-compose up -d
```

---

### 方式二：本地 Nginx

1. **安装 Nginx**

- Windows: 下载 https://nginx.org/en/download.html
- Linux: `sudo apt-get install nginx` 或 `sudo yum install nginx`

2. **复制文件**

```bash
# 复制 404 页面
cp frontend/public/404.html /usr/share/nginx/html/

# 复制配置文件
cp nginx.conf /etc/nginx/conf.d/default.conf
```

3. **修改配置中的路径**

编辑 `nginx.conf`，将路径改为实际路径：
```nginx
root /path/to/market/frontend/build;
```

4. **重启 Nginx**

```bash
# Linux
sudo nginx -s reload

# Windows
nginx -s reload
```

---

## 🎨 404 页面特性

- ✅ 响应式设计（支持手机、平板、桌面）
- ✅ 渐变背景动画
- ✅ 漂浮装饰元素
- ✅ 返回首页按钮
- ✅ 控制台日志记录

---

## 🔧 自定义配置

### 修改 404 页面文字

编辑 `frontend/public/404.html`：

```html
<div class="error-code">404</div>
<h1 class="error-message">页面走丢了</h1>  <!-- 修改这里 -->
<p class="error-description">
    抱歉，您访问的页面不存在或已被移除。<br>
    请检查网址是否正确，或返回首页继续浏览。
</p>
```

### 修改配色方案

```css
body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* 修改这里的颜色 */
    /* 例如：background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); */
}

.home-btn {
    background: #fff;
    color: #667eea;  /* 修改按钮文字颜色 */
}
```

### 添加日志收集

取消注释 `404.html` 中的 JavaScript 代码：

```javascript
// 发送到日志收集服务
fetch('/api/log/404', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        url: window.location.href,
        referrer: document.referrer,
        timestamp: new Date().toISOString()
    })
});
```

---

## 📝 Nginx 配置说明

### 关键配置项

```nginx
# 自定义 404 错误页面
error_page 404 /404.html;

# 404 页面位置
location = /404.html {
    root /usr/share/nginx/html;
    internal;  # 只能内部跳转访问
}

# SPA 路由支持
location / {
    try_files $uri $uri/ /index.html;
}
```

### 常见错误码配置

```nginx
# 自定义错误页面
error_page 400 /400.html;
error_page 401 /401.html;
error_page 403 /403.html;
error_page 404 /404.html;
error_page 500 502 503 504 /50x.html;

location = /400.html { root /usr/share/nginx/html; internal; }
location = /401.html { root /usr/share/nginx/html; internal; }
location = /403.html { root /usr/share/nginx/html; internal; }
location = /404.html { root /usr/share/nginx/html; internal; }
location = /50x.html { root /usr/share/nginx/html; internal; }
```

---

## 🧪 测试方法

1. **访问不存在的页面**
```
http://localhost/not-exist-page
http://localhost/abc/def/ghi
```

2. **检查响应**
- 状态码应为 404
- 显示自定义 404 页面
- 点击"返回首页"能正常跳转

3. **查看日志**
```bash
# Nginx 访问日志
tail -f /var/log/nginx/access.log

# Nginx 错误日志
tail -f /var/log/nginx/error.log
```

---

## ⚠️ 注意事项

1. **路径问题**：确保 `root` 路径指向正确的目录
2. **权限问题**：确保 Nginx 用户有权限读取 404.html 文件
3. **缓存问题**：修改后清除浏览器缓存或强制刷新（Ctrl+F5）
4. **internal 指令**：防止用户直接访问 /404.html

---

## 🔗 相关资源

- [Nginx 官方文档 - 错误页面](https://nginx.org/en/docs/http/ngx_http_core_module.html#error_page)
- [Nginx 配置最佳实践](https://www.nginx.com/resources/admin-guide/nginx-beginner-guide/)
